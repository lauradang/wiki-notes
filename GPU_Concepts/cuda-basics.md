# CUDA Basics

| | CPU | GPU |
| --- | --- | --- |
| Name | Host | Device |
| Unit of work | Function | Kernel |

A **kernel** is a GPU function that can be parallelized. The host (CPU) executes functions; the device (GPU) executes kernels.
